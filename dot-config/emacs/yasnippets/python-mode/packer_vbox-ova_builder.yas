# -*- mode: snippet -*-
# contributor: dandy
# key: PCK_BUILD_VBOX_OVA
# name: packer builder vbox ova
# group: packer
# keybinding:

# --
{
    "type": "${1:virtualbox-ovf}",
    "source_path": "${2:/path/to/source.ovf}",
    "ssh_username": "${3:packer}",
    "ssh_password": "${4:packer}",
    "shutdown_command": "${5:echo 'packer' | sudo -S shutdown -P now}"
}